
export enum Status {
    pending = "Pending",
    confirmed = "Confirmed",
    cancelled = "Cancelled"
}

export enum Role {
    admin = "Admin",
    store = "Store",
    client = "Client",
    moderator = "Moderator"
}